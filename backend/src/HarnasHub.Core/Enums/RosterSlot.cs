namespace HarnasHub.Core.Enums;

/// <summary>Where a player sits in the roster: the starting five, the bench, or a temporary stand-in who doesn't appear in the availability calendar.</summary>
public enum RosterSlot
{
	Main = 0,
	Bench = 1,
	StandIn = 2
}
